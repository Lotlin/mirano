export const body = document.body;

export const header = body.querySelector('.header');

// export const allChoices = body.querySelectorAll('.choices');
export const allChoicesBtns = body.querySelectorAll('.choices__btn');
export const allChoicesBoxes = body.querySelectorAll('.choices__box');

/*
export const getChoicesElements = (choice) => {
  const btn = choice.querySelector('.choices__btn');
  const box = choice.querySelector('.choices__box');

  return {
    btn,
    box,
  };
};
*/
